name: Publish release

on:
  push:
    tags:
      - "v*"
  workflow_dispatch:
    inputs:
      tag:
        description: "Release tag, for example v1.0.0"
        required: true
        type: string

permissions:
  contents: write

jobs:
  release:
    runs-on: ubuntu-latest
    env:
      RELEASE_TAG: ${{ inputs.tag || github.ref_name }}
    steps:
      - uses: actions/checkout@v7

      - uses: actions/setup-node@v7
        with:
          node-version: 22
          cache: npm
          cache-dependency-path: frontend/package-lock.json

      - name: Build frontend
        run: |
          npm --prefix frontend ci
          npm --prefix frontend run build

      - name: Package release
        run: |
          mkdir -p package/acm-nav/frontend package/acm-nav/static
          cp -R backend package/acm-nav/
          cp -R frontend/dist/. package/acm-nav/frontend/
          cp -R frontend/dist/icons frontend/dist/resources package/acm-nav/static/
          rm -rf package/acm-nav/frontend/icons package/acm-nav/frontend/resources
          cp requirements.txt README.md LICENSE package/acm-nav/
          cd package/acm-nav
          zip -qr "../../acm-nav-${RELEASE_TAG}.zip" .

      - name: Create GitHub Release
        env:
          GH_TOKEN: ${{ github.token }}
        run: gh release create "$RELEASE_TAG" "acm-nav-${RELEASE_TAG}.zip" --target "$GITHUB_SHA" --title "$RELEASE_TAG" --generate-notes
